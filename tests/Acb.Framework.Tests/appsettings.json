{
  "mode": "dev",
  "config": {
    "application": "sites,dapper,redis,mongo",
    "uri": "http://192.168.0.252:8888"
  },
  //扩展的配置文件路径(json)
  "configPath": "_config",
  //默认的Redis连接名
  "redisDefault": "default",
  //默认的Mongo连接名
  "mongoDefault": "default"
}
